/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Gym Buddy API
 * Canonical HTTP contract. Edit this $ref tree. Generators read
 * `openapi/openapi.yaml` on a tagged package checkout (or a develop SHA
 * pin until the next 0.1.x tag). Do not treat a running Spring
 * /v3/api-docs as source of truth.
 *
 * OpenAPI spec version: 1.0.0
 */
import type { GetConversations200DataItemLastMessage } from './getConversations200DataItemLastMessage';
import type { GetConversations200DataItemPeer } from './getConversations200DataItemPeer';

/**
 * Direct conversation in the caller's inbox (FS-MSG-01, FS-MSG-09). One row
 * per unordered friend pair. `peer` is the other participant. `unreadCount`
 * is messages from the peer since the caller last GET the thread.
 * `updatedAt` is last-message time (or created time if empty).
 */
export type GetConversations200DataItem = {
  id: string;
  /** The other participant in a direct conversation, relative to the caller. */
  peer: GetConversations200DataItemPeer;
  /**
   * Private message (FS-MSG-03..08). `type` is `text`, `image`, or `audio`.
   * Text body is 1–4000 characters. Image/audio reference one `mediaId`
   * (`kind=message`). Author delete within 10 minutes tombstones `body` as
   * `message deleted` and sets `deleted`; the original body and media are never
   * returned afterwards. Signed bytes are fetched via GET /media/{id}/url.
   */
  lastMessage?: GetConversations200DataItemLastMessage;
  /** @minimum 0 */
  unreadCount: number;
  updatedAt: string;
};
