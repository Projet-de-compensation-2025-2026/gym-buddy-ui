/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Gym Buddy API
 * Canonical HTTP contract. Edit this $ref tree. Generators read
 * `openapi/openapi.yaml` on a tagged package checkout (or a develop SHA
 * pin until the next 0.1.x tag). Do not treat a running Spring
 * /v3/api-docs as source of truth.
 *
 * OpenAPI spec version: 1.1.1
 */
import type { GetMatchingMe200Event } from './getMatchingMe200Event';
import type { GetMatchingMe200Pair } from './getMatchingMe200Pair';

/**
 * Caller's weekly matching state (FS-MATCH-01..03). `pair` and `event` are
 * present after the greedy job assigns this member; both are omitted (or
 * null) when unmatched. Opt-in is per ISO week starting Monday UTC.
 */
export type GetMatchingMe200 = {
  optedIn: boolean;
  /** Monday of the current ISO week (UTC). */
  weekStart: string;
  /** The other member on this week's proposed buddy pair (FS-MATCH-02). */
  pair?: GetMatchingMe200Pair;
  /**
   * Proposed instant session for a weekly match (FS-MATCH-03). Draft,
   * private invitation for the matched peer, capacity 1. `id` is set when an
   * events row exists; the matched peer applies and the organizer accepts
   * through the events UI.
   */
  event?: GetMatchingMe200Event;
};
