/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Gym Buddy API
 * Canonical HTTP contract. Edit this $ref tree. Generators read
 * `openapi/openapi.yaml` on a tagged package checkout (or a develop SHA
 * pin until the next 0.1.x tag). Do not treat a running Spring
 * /v3/api-docs as source of truth.
 *
 * OpenAPI spec version: 0.1.0
 */
import type { GetFeed200DataItemActor } from './getFeed200DataItemActor';
import type { GetFeed200DataItemKind } from './getFeed200DataItemKind';
import type { GetFeed200DataItemPost } from './getFeed200DataItemPost';

/**
 * Friends feed activity (FS-FEED-01..06). `kind` is `post` (authored by the
 * viewer or an accepted friend) or `repost` (they reposted a post the viewer
 * may see). `actor` is the author or the reposter. `activityAt` is the post
 * `createdAt` or the repost `createdAt`. `post` is the original; hidden or
 * deleted originals omit the whole item.
 */
export type GetFeed200DataItem = {
  /** Post id when `kind=post`; unique repost row id when `kind=repost`. */
  id: string;
  kind: GetFeed200DataItemKind;
  /** Author shown on a post card (FS-POST). */
  actor: GetFeed200DataItemActor;
  activityAt: string;
  /**
   * Member post (FS-POST-01..08). `visibility` is `friends` (default) or `public`.
   * Hidden or deleted posts are `NOT_FOUND` to members. `mediaIds` are `kind=post`
   * image rows owned by the author and `ready` (max 4). `commentCount` includes
   * tombstones.
   */
  post: GetFeed200DataItemPost;
};
