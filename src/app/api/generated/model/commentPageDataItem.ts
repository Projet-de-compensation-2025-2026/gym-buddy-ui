/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Gym Buddy API
 * Canonical HTTP contract. Edit this $ref tree. Generators read
 * `openapi/openapi.yaml` on a tagged package checkout (or a develop SHA
 * pin until the next 0.1.x tag). Do not treat a running Spring
 * /v3/api-docs as source of truth.
 *
 * OpenAPI spec version: 1.0.0
 */
import type { CommentPageDataItemAuthor } from './commentPageDataItemAuthor';

/**
 * Nested comment on a post (FS-CMT-01..07). Root comments omit `parentId`
 * (`depth` 0). A reply stores `depth = parent.depth + 1`. Maximum depth is 4
 * (root = 0). Author delete tombstones `body` as `comment deleted` and sets
 * `deleted`; children remain. Hidden comments also tombstone. No media at MVP.
 */
export type CommentPageDataItem = {
  id: string;
  postId: string;
  /** @nullable */
  parentId?: string | null;
  /** Author shown on a post card (FS-POST). */
  author: CommentPageDataItemAuthor;
  /**
   * 1–1000 characters. When `deleted` is true this is the tombstone
   * `comment deleted`; the original body is never returned.
   * @minLength 1
   * @maxLength 1000
   */
  body: string;
  /**
   * @minimum 0
   * @maximum 4
   */
  depth: number;
  createdAt: string;
  /** True after the author tombstones the comment. */
  deleted: boolean;
  /** @minimum 0 */
  likeCount: number;
  /** Whether the caller currently likes this comment. */
  liked: boolean;
  /**
   * Direct children, including tombstones. Expand via GET /comments/{id}/replies.
   * @minimum 0
   */
  replyCount: number;
};
